/* Train Hard — Premium / Platega SBP configuration. */
(function () {
  'use strict';
  window.TRAINHARD_PREMIUM = {
    priceLabel: '299 ₽',
    periodDays: 30,
    payments: {
      enabled: true,
      provider: 'platega',
      currency: 'RUB'
    }
  };
})();